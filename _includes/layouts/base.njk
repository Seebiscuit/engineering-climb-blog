<!doctype html>
<html lang="en" {% if templateClass %} class="{{ templateClass }}" {% endif %}>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/style.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
  </head>
  <body>
    <header>
      <section id="mainHeaderSection" 
          class="
            pattern-topography 
            pt-2 pb-3
            mb-3 sm:mb-4 
            w-full">
        <h1 id="mainHeaderTitle" 
            class="
              text-xl sm:text-3xl font-bold 
              inline-block 
              ml-3 sm:ml-5 md:ml-0">
          <a href="{{ '/' | url }}">{{ metadata.title }}</a>
        </h1>
        <input type="checkbox" name="top-menu-checkbox" id="topMenuCheckbox" class="appearance-none sm:hidden align-bottom w-5 h-5 mr-5 mb-1">
        <ul class="nav hidden sm:flex sm:justify-between w-full sm:w-64 h-15 bg-white sm:bg-transparent
            bg-opacity-60 sm:bg-opacity-100
            pt-1 sm:pt-3 
            sm:pr-5 pdr-3 
            pb-7
            px-5 
            sm:float-right sm:-m-2">
          {%- for entry in collections.all | eleventyNavigation %}
            <li class="
                font-semibold
                nav-item{% if entry.url == page.url %} nav-item-active{% endif %}">
              <a href="{{ entry.url | url }}">{{ entry.title }}</a>
            </li>
          {%- endfor %}
        </ul>
        <h2 id="mainHeaderSubTitle" 
            class="
            header-subtitle 
            hidden"
          >Ideas to level up our software systems</h2>
      </section>
      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
    </header>
    <main 
        class="
          ml-3 sm:ml-5 
          mr-3 sm:mr-5 
          w-full md:w-192
          md:mx-auto">
      {{ content | safe }}
    </main>
    <footer></footer>
    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>